<div class="table-responsive">
  <table class="table table-bordered table-condensed">
  <thead>
    <tr>
      <th colspan="100%">
        <%= link_to @survey.name, edit_customer_survey_path(@customer, @survey) %>
      </th>
    </tr>
    <tr>
      <th>Order</th>
      <th>Text</th>
      <th>Question Type</th>
      <th>Move Up/Down</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% @survey.questions.order(:index).each do |question| %>
  <tr>
    <td>
      <div class="divLink">
        <%= question.index %>
      </div>
    </td>
    <td>
      <div class="divLink">
        <%= question.text %>
      </div>
    </td>
    <td>
      <div class="divLink">
        <%= question.yes_no? ? "Yes/No" : question.rating? ? "Rating" : "Free Form" %>
      </div>
    </td>
    <td>
      <% if question.index > 1 %>
        <%= link_to '&uarr;'.html_safe, up_customer_survey_question_path(@customer,@survey,question) %>
      <% end %>
      <% if question.index < @survey.questions.size %>
        <%= link_to "&darr;".html_safe, down_customer_survey_question_path(@customer,@survey,question) %>
      <% end %>

    </td>
    <td>
      <%= link_to 'Edit', edit_customer_survey_question_path(@customer,@survey,question)%> |
      <%= link_to 'Delete', customer_survey_question_path(@customer,@survey,question), method: :delete, data: { confirm: 'Are you sure?' } %>
    </td>
  </tr>
  <% end %>
  <tr>
    <td colspan="100%">
      <%= link_to 'New Question', new_customer_survey_question_path(@customer, @survey) %>
    </td>
  </tr>
  <tr>
    <td>
      <div class="divLink">
        <%= link_to "Completed Message:", edit_customer_survey_path(@customer, @survey) %>
      </div>
    </td>
    <td>
      <div class="divLink">
      <pre>
<%= link_to @survey.complete_message, edit_customer_survey_path(@customer, @survey) %>
      </pre>
      </div>
    </td>
  </tr>
  <!--<tr>-->
    <!--<td>-->
  <!--QR code for this survey:-->
    <!--</td>-->
    <!--<td>-->
      <!--<% if @qr %>-->
        <!--<table cellpadding=0 cellspacing=0 style="border: 2px solid white ">-->
          <!--<% @qr.modules.each_index do |x| %>-->
            <!--<tr>-->
              <!--<% @qr.modules.each_index do |y| %>-->
                <!--<% if @qr.dark?(x,y) %>-->
                  <!--<td class="black" style="background-color: black;height:2px; width:2px;"/>-->
                <!--<% else %>-->
                  <!--<td class="white" style="background-color: white;height:2px;width:2px;"/>-->
                <!--<% end %>-->
              <!--<% end %>-->
            <!--</tr>-->
          <!--<% end %>-->
        <!--</table>-->
      <!--<% end %>-->
    <!--</td>-->
  <!--</tr>-->
  </tbody>
  </table>
</div>
<div class="table-responsive">
  <table class="table table-bordered table-condensed">
    <thead>
    <tr>
      <td colspan="100%">
        <div style="font-weight: bold">View and Print Survey Cards</div>
        <div style="font-size: small">Print surveys are first 4 questions only. Links open new window.</div>
      </td>
    </tr>
    </thead>
    <tbody>
    <% if @reporting_fields %>
    <% @reporting_fields.each do |rf| %>
      <tr>
        <td></td>
          <td>
            <% p = {format: 'pdf'}
               rf.each do |f|
                 p = p.merge(f)
               end %>
            <%= link_to p.select{|k| k.match /^c_/}.to_s.gsub(/[{}\"c_>]/,""), card_customer_survey_path(@customer, @survey, p), target: "_blank" %>
          </td>
      </tr>
    <% end %>
    <% end %>
    <tr>
      <td></td>
      <td>
        <%= link_to "No Reporting Fields", card_customer_survey_path(@customer, @survey, format: 'pdf'), target: "_blank" %>
      </td>
    </tr>
    <tr>
      <td></td>
      <td><%= link_to "View Web Survey", complete_surveys_path(survey_id: @survey.id), target: "_blank"%></td>
    </tr>
    </tbody>
  </table>
</div>